巡检系统软件设计书

一、文档目标
本文档用于说明巡检系统的功能范围、技术架构、数据模型、接口设计、实施计划与验收标准。
目标是把“项目简介”升级为“可开发、可联调、可验收”的执行文档。

二、项目概述
本项目构建一套智能化、可视化的巡检管理系统，通过移动端 App 与 Web 端协同，实现巡检员、管理员、访客三类角色在同一业务闭环内协作。

系统核心数据对象是“风险事件”，围绕“发现-上报-处理-归档”流程沉淀业务数据，并通过地图与实时通信展示巡检过程。



三、角色与职责
1. 巡检员（inspector）
- 使用移动端登录。
- 按路线巡检，在检查点打卡。
- 发现风险后上报照片、位置、等级、描述。

2. 管理员（admin）
- 管理用户账号与权限。
- 查看在线巡检员及实时位置。
- 处理风险并归档。

3. 访客（viewer）
- 查看地图态势、在线人员和风险分布。
- 无数据编辑权限。

四、系统架构
1. 前端
- Web 端：React + Vite。
- App 端：React + Vite + Capacitor（Android）。

2. 后端
- Express + express-session。
- REST API 负责登录、用户管理、风险管理等事务型接口。
- Socket.IO 负责实时心跳与位置上报。

3. 数据存储
- 业务库：SQLite（当前已落地）。
- 空间库：SpatiaLite。

五、当前实现现状（基于代码）
1. 已完成
- 登录、退出、会话态接口。
- 按角色鉴权。
- 用户管理接口：增删改查。
- 默认用户初始化：admin/inspector/viewer。
- Socket 连接与心跳机制。
- 巡检员位置上报并在在线用户列表展示。
- Web 管理端：用户管理页面、在线用户页面。
- Web 访客端：在线用户页面。
- App 巡检员端：登录、心跳发送、位置上报。

2. 部分完成
- 管理端“风险处理”页面目前为占位页面，尚未接入真实接口。

3. 未完成
- 路线管理（增删改查、地图绘制、发布）。
- 检查点管理与打卡规则。
- 风险事件完整闭环（上报、指派、处理、归档）。
- 地图底图与轨迹可视化。
- 照片上传与文件存储服务。
- PostGIS 空间模型与空间查询。

六、功能设计
1. 认证与权限
- 登录成功后返回 sessionId，并由 Cookie 维持会话。
- 所有管理接口通过角色控制：admin、inspector、viewer。

2. 在线监控
- 巡检员 App 通过 Socket 周期发送 heartbeat。
- 心跳携带 sessionId 与位置信息。
- 管理端按轮询展示在线用户及最近定位时间。

3. 风险闭环（规划）
- 风险上报：巡检员提交描述、等级、定位、照片。
- 风险分派：管理员确认并分配处理责任人。
- 风险处理：填写处理措施、完成时间、结果说明。
- 风险归档：状态改为已解决，保留审计轨迹。

4. 路线与检查点（规划）
- 路线：可创建、编辑、停用、发布。
- 检查点：属于路线，具备顺序、坐标、打卡半径。
- 打卡校验：根据巡检员定位与检查点半径判定通过与否。

七、数据模型设计
1. 用户表 Users（当前已有）
- UserID INTEGER PRIMARY KEY AUTOINCREMENT
- Name TEXT NOT NULL
- Password TEXT NOT NULL（建议由明文迁移为哈希）
- Role TEXT NOT NULL（admin/inspector/viewer）

2. 路线表 Routes（规划）
- RouteID INTEGER PRIMARY KEY AUTOINCREMENT
- Name TEXT NOT NULL
- Status TEXT NOT NULL（draft/active/inactive）
- CreatedBy INTEGER NOT NULL
- CreatedAt DATETIME NOT NULL
- UpdatedAt DATETIME NOT NULL

3. 检查点表 Checkpoints（规划）
- CheckpointID INTEGER PRIMARY KEY AUTOINCREMENT
- RouteID INTEGER NOT NULL
- Name TEXT NOT NULL
- SeqNo INTEGER NOT NULL
- Longitude REAL NOT NULL
- Latitude REAL NOT NULL
- RadiusMeter REAL NOT NULL DEFAULT 30
- MustCheck INTEGER NOT NULL DEFAULT 1
- Description TEXT

4. 风险表 Risks（规划）
- RiskID INTEGER PRIMARY KEY AUTOINCREMENT
- ReporterUserID INTEGER NOT NULL
- RouteID INTEGER
- CheckpointID INTEGER
- Address TEXT
- Longitude REAL NOT NULL
- Latitude REAL NOT NULL
- PhotoURL TEXT
- Description TEXT NOT NULL
- RiskLevel TEXT NOT NULL（low/medium/high）
- Status TEXT NOT NULL（open/resolved）
- ReportedAt DATETIME NOT NULL
- ResolvedAt DATETIME
- ResolvedByUserID INTEGER
- ResolveNote TEXT

5. 打卡记录表 CheckinRecords（规划）
- CheckinID INTEGER PRIMARY KEY AUTOINCREMENT
- CheckpointID INTEGER NOT NULL
- UserID INTEGER NOT NULL
- CheckinTime DATETIME NOT NULL
- Longitude REAL NOT NULL
- Latitude REAL NOT NULL
- DistanceMeter REAL
- Result TEXT NOT NULL（pass/fail）
- PhotoURL TEXT
- Note TEXT

八、接口设计
1. 接口通用约定
- Base URL：http://localhost:1145（部署后按域名或网关地址替换）。
- 数据格式：application/json。
- 认证方式：Session Cookie（your_app_session）。
- 时间格式：ISO 8601（例如 2026-04-20T10:30:00.000Z）。
- 统一响应结构：
	成功：{ success: true, ...业务字段 }
	失败：{ success: false, error: "错误信息", code: "错误码(可选)" }

2. 鉴权与会话接口（已完成）
1. POST /login
- 权限：匿名可访问。
- 请求体：
	username: string，必填。
	password: string，必填。
- 成功响应（200）：
	success: true
	user: { id, username, roles, EmployeeID }
	sessionId: string
- 失败响应：
	401：用户名或密码错误。
	400：参数缺失（建议补齐）。

2. POST /logout
- 权限：已登录。
- 请求体：无。
- 成功响应（200）：
	success: true
- 失败响应：
	401：未登录。
	500：退出失败。

3. GET /me
- 权限：已登录。
- 请求参数：无。
- 成功响应（200）：
	user: { id, username, roles, EmployeeID }
- 失败响应：
	401：未登录。

3. 在线用户接口（已完成）
1. GET /online-users
- 权限：admin、viewer。
- 请求参数：无。
- 成功响应（200）：
	success: true
	count: number
	users: [
		{
			sessionId,
			id,
			username,
			roles,
			EmployeeID,
			loginAt,
			socketId,
			lastHeartbeatAt,
			location: { latitude, longitude, accuracy } | null,
			locationUpdatedAt
		}
	]
- 失败响应：
	401：未登录。
	403：权限不足。

4. 用户管理接口（已完成）
1. GET /users
- 权限：admin。
- 成功响应（200）：
	success: true
	users: [{ id, username, password, roles, EmployeeID }]
- 失败响应：401/403/500。

2. POST /users
- 权限：admin。
- 请求体：
	username: string，必填，唯一。
	password: string，必填。
	roles: enum(admin, inspector, viewer)。
- 成功响应（201）：
	success: true
	user: { id, username, roles, EmployeeID }
- 失败响应：
	400：字段校验失败/角色非法/用户名重复。
	401：未登录。
	403：权限不足。

3. PUT /users/:id
- 权限：admin。
- 路径参数：id，正整数。
- 请求体：
	username: string，必填。
	password: string，必填。
	roles: enum(admin, inspector, viewer)。
- 成功响应（200）：
	success: true
	user: { id, username, password, roles, EmployeeID }
- 失败响应：
	400：参数非法/角色非法/用户名重复。
	404：用户不存在。
	401/403：鉴权失败。

4. DELETE /users/:id
- 权限：admin。
- 路径参数：id，正整数。
- 成功响应（200）：
	success: true
	deletedUserId: number
- 失败响应：
	400：ID 非法。
	404：用户不存在。
	401/403：鉴权失败。

5. 路线管理接口（规划，未完成）
1. GET /routes
- 权限：admin、viewer、inspector。
- 查询参数：
	status: draft|active|inactive（可选）
	page: number（可选，默认 1）
	pageSize: number（可选，默认 20）
- 成功响应（200）：
	success: true
	total: number
	routes: [{ RouteID, Name, Status, CreatedBy, CreatedAt, UpdatedAt }]

2. POST /routes
- 权限：admin。
- 请求体：
	name: string，必填。
	status: draft|active|inactive（可选，默认 draft）。
- 成功响应（201）：
	success: true
	route: { RouteID, Name, Status, CreatedBy, CreatedAt, UpdatedAt }

3. PUT /routes/:id
- 权限：admin。
- 请求体：
	name: string（可选）
	status: draft|active|inactive（可选）
- 成功响应（200）：
	success: true
	route: {...}

4. DELETE /routes/:id
- 权限：admin。
- 说明：逻辑删除优先，建议改为 inactive。
- 成功响应（200）：
	success: true
	routeId: number

6. 检查点管理接口（规划，未完成）
1. GET /routes/:routeId/checkpoints
- 权限：admin、viewer、inspector。
- 成功响应（200）：
	success: true
	checkpoints: [
		{ CheckpointID, RouteID, Name, SeqNo, Longitude, Latitude, RadiusMeter, MustCheck, Description }
	]

2. POST /routes/:routeId/checkpoints
- 权限：admin。
- 请求体：
	name: string，必填。
	seqNo: number，必填。
	longitude: number，必填。
	latitude: number，必填。
	radiusMeter: number（可选，默认 30）。
	mustCheck: 0|1（可选，默认 1）。
	description: string（可选）。
- 成功响应（201）：
	success: true
	checkpoint: {...}

3. PUT /checkpoints/:id
- 权限：admin。
- 请求体：可更新 name、seqNo、longitude、latitude、radiusMeter、mustCheck、description。
- 成功响应（200）：success: true, checkpoint: {...}

4. DELETE /checkpoints/:id
- 权限：admin。
- 成功响应（200）：success: true, checkpointId: number

7. 打卡接口（规划，未完成）
1. POST /checkins
- 权限：inspector。
- 请求体：
	checkpointId: number，必填。
	longitude: number，必填。
	latitude: number，必填。
	photoUrl: string（可选）。
	note: string（可选）。
- 服务端处理：计算与检查点中心距离，写入 DistanceMeter 与 Result(pass/fail)。
- 成功响应（201）：
	success: true
	checkin: { CheckinID, CheckpointID, UserID, CheckinTime, DistanceMeter, Result }

2. GET /checkins
- 权限：admin、inspector。
- 查询参数：
	userId: number（可选）
	routeId: number（可选）
	startAt/endAt: datetime（可选）
- 成功响应（200）：success: true, total, records: []

8. 风险接口（规划，未完成）
1. POST /risks
- 权限：inspector。
- 请求体：
	routeId: number（可选）
	checkpointId: number（可选）
	address: string（可选）
	longitude: number，必填。
	latitude: number，必填。
	photoUrl: string（可选）
	description: string，必填。
	riskLevel: low|medium|high，必填。
- 成功响应（201）：
	success: true
	risk: { RiskID, ReporterUserID, Status: open, ReportedAt, ... }

2. GET /risks
- 权限：admin、viewer、inspector（inspector 仅可看自己上报记录，建议）。
- 查询参数：
	status: open|processing|resolved（可选）
	riskLevel: low|medium|high（可选）
	page/pageSize（可选）
	startAt/endAt（可选）
- 成功响应（200）：success: true, total, risks: []

3. GET /risks/:id
- 权限：admin、viewer、inspector(受数据权限约束)。
- 成功响应（200）：success: true, risk: {...}
- 失败响应：404（不存在）

4. PUT /risks/:id
- 权限：admin。
- 请求体（建议分状态更新）：
	status: open|processing|resolved
	resolvedByUserId: number（resolved 时必填）
	resolveNote: string（可选）
- 成功响应（200）：success: true, risk: {...}

9. 文件上传接口（规划，未完成）
1. POST /uploads/risk-photo
- 权限：inspector、admin。
- Content-Type：multipart/form-data。
- 表单字段：file。
- 成功响应（201）：
	success: true
	url: string
	filename: string
	size: number
- 失败响应：
	400：文件格式或大小不合法。
	413：文件过大。

10. 健康检查接口（建议新增，未完成）
1. GET /health
- 权限：匿名可访问（或仅内网）。
- 成功响应（200）：
	success: true
	status: "ok"
	version: "x.y.z"
	time: datetime

11. 接口错误码建议
- AUTH_REQUIRED：未登录。
- PERMISSION_DENIED：权限不足。
- INVALID_PARAM：参数校验失败。
- NOT_FOUND：资源不存在。
- CONFLICT：数据冲突（例如用户名重复）。
- INTERNAL_ERROR：服务端异常。

九、实时通信设计
1. 事件
- 客户端 -> 服务端：heartbeat
- 客户端 -> 服务端：text_update（当前用于测试）
- 服务端 -> 客户端：heartbeat_ack

2. 约束
- 未绑定有效 sessionId 的 heartbeat 不计入在线态。
- 在线态依据最后心跳时间判定，可配置超时阈值（建议 30 秒）。

十、非功能设计
1. 安全
- 角色最小权限。
- Cookie 开启 httpOnly。
- 生产环境启用 secure + sameSite=none（已具备开关）。
- 密码改为哈希存储（未完成，优先级高）。

2. 可维护性
- 接口统一返回 success/error 结构。
- 关键行为记录日志（登录、风险状态变更、删除操作）。

3. 性能
- 在线列表查询目标：小规模并发下 1 秒内返回。
- 风险列表支持分页与条件筛选。

十一、测试与验收
1. 测试范围
- 认证鉴权测试。
- 用户管理接口测试。
- 心跳与在线状态测试。
- 风险闭环接口测试（待开发后补齐）。

2. 验收标准
- 三角色登录权限正确。
- 在线用户及位置可展示。
- 用户管理完整可用。
- 风险上报到归档链路跑通。
- 检查点打卡结果可追溯。

十二、实施计划（建议）
第一阶段（已完成主体）
- 认证与角色体系。
- 在线用户与心跳链路。
- 用户管理基础后台。

第二阶段（建议 1-2 周）
- 落地路线表与检查点表。
- 完成检查点管理接口与页面。
- 打卡记录接口与巡检端打卡页面。

第三阶段（建议 1-2 周）
- 落地风险表、风险接口与管理端风险处理页。
- App 端风险上报（含图片上传）。
- 风险地图点位展示与筛选。

第四阶段（建议 1 周）
- PostGIS 路线与空间能力接入。
- 性能优化、日志完善、联调验收。

十三、已知风险与应对
1. 密码明文存储风险
- 应对：迁移为 bcrypt 哈希并增加密码策略。

2. 在线用户仅内存存储，服务重启丢失
- 应对：将在线态作为临时状态可接受；关键轨迹落库。

3. Socket 与 Session 绑定依赖前端 sessionId
- 应对：增加服务端校验与异常告警。

十四、后续文档拆分建议
1. 新增接口文档（OpenAPI 格式）。
2. 新增数据库迁移文档（DDL 版本化）。
3. 新增部署运维手册（环境变量、反向代理、HTTPS）。

十五、三端运行流程（含完成度）
为便于开发、联调和演示，以下按“巡检员 App 端、后端服务端、Web 端（管理员/访客）”给出运行流程，并标注完成情况。

1. 巡检员 App 端运行流程
- 步骤 1：启动 App，进入登录页，输入账号密码调用 POST /login。
	状态：已完成。
- 步骤 2：登录成功后保存用户信息与 sessionId，基于角色跳转到巡检页面。
	状态：已完成。
- 步骤 3：建立 Socket.IO 连接，周期发送 heartbeat（附 sessionId）。
	状态：已完成。
- 步骤 4：获取设备定位（watchPosition），位置变化或定时触发时上报经纬度。
	状态：已完成。
- 步骤 5：在巡检页面进行检查点打卡（校验半径、记录结果）。
	状态：未完成。
- 步骤 6：风险上报（拍照、描述、等级、位置）并提交风险接口。
	状态：未完成。
- 步骤 7：查看个人巡检历史、打卡历史与风险处理反馈。
	状态：未完成。

2. 后端服务端运行流程
- 步骤 1：启动 Express 服务，加载 CORS、session、静态资源与路由。
	状态：已完成。
- 步骤 2：连接 SQLite 数据库并初始化 Users 表与默认账户。
	状态：已完成。
- 步骤 3：提供认证接口（/login、/logout、/me）与角色鉴权。
	状态：已完成。
- 步骤 4：提供用户管理接口（/users 增删改查）。
	状态：已完成。
- 步骤 5：接收 heartbeat，绑定 session 与 socket，更新在线状态与位置缓存。
	状态：已完成。
- 步骤 6：提供路线、检查点、打卡、风险相关 REST 接口。
	状态：未完成。
- 步骤 7：提供图片上传能力（风险图片）并返回可访问地址。
	状态：未完成。
- 步骤 8：风险闭环状态流转（open -> processing -> resolved）与审计日志。
	状态：未完成。

3. Web 端运行流程（管理员/访客）
- 步骤 1：打开 Web 页面并登录，按角色进入 admin 或 viewer 路由。
	状态：已完成。
- 步骤 2：管理员进入用户管理页面，进行新增、修改、删除、查询。
	状态：已完成。
- 步骤 3：管理员/访客进入在线用户页面，查看在线人数、位置与心跳时间。
	状态：已完成。
- 步骤 4：管理员进入风险处理页面，查看风险列表并执行处置/归档。
	状态：部分完成（页面入口已存在，业务页面和接口未接入）。
- 步骤 5：在地图页面查看巡检路线、检查点、巡检员轨迹与风险分布。
	状态：未完成。
- 步骤 6：按时间、等级、状态进行风险筛选统计并导出报表。
	状态：未完成。

4. 三端联调主链路（目标流程）
- 链路 A：App 登录并发送 heartbeat -> 后端更新在线状态 -> Web 在线用户页面展示。
	状态：已完成。
- 链路 B：App 打卡 -> 后端写入 CheckinRecords -> Web 地图显示检查点完成状态。
	状态：未完成。
- 链路 C：App 风险上报 -> 后端入库 Risks -> Web 管理端处置并归档 -> 访客端可见风险分布。
	状态：未完成。

5. 当前可演示范围与下一步
- 当前可演示：登录鉴权、用户管理、在线用户与实时位置上报。
- 优先补齐建议：先完成“路线+检查点+打卡链路”，再完成“风险上报+处理归档链路”，最后补地图可视化和统计导出。

十六、项目开发与管理方法
本节用于统一项目协作方式，目标是做到“需求可追踪、开发可并行、上线可回滚、问题可复盘”。

1. 开发方法
- 采用小步迭代开发（按周交付），每次迭代只聚焦 1-2 条主链路。
- 以“业务闭环”为优先级：先打通链路，再做体验优化。
- 推荐迭代顺序：认证与在线态 -> 路线与打卡 -> 风险闭环 -> 地图分析与报表。

2. 需求与任务管理
- 需求来源统一进入需求池，按 P0/P1/P2 分级。
- 每条需求拆分为可验收任务，明确输入、输出、接口、验收标准。
- 任务状态建议使用：待开发、开发中、联调中、待测试、已完成。
- 每周固定一次需求澄清会，减少实现偏差。

3. 分支与版本管理
- 建议采用主干开发 + 短分支策略。
- 分支命名建议：feature/xxx、fix/xxx、hotfix/xxx。
- 合并规则：至少 1 人 Code Review 通过后方可合并。
- 提交信息建议采用规范前缀：feat、fix、refactor、docs、test、chore。

4. 联调与接口管理
- 接口先定义后开发，优先维护统一接口文档（建议 OpenAPI）。
- 前后端并行时，优先使用 mock 数据保持页面开发连续性。
- 联调前执行接口自测清单：鉴权、参数校验、错误码、边界值。

5. 测试管理
- 测试分层：单元测试、接口测试、端到端主链路测试。
- 回归重点：登录鉴权、在线用户、用户管理、风险状态流转。
- 每次迭代末执行一次冒烟测试，确认核心链路不回退。
- 缺陷处理建议遵循：提交缺陷 -> 分级 -> 修复 -> 回归 -> 关闭。

6. 发布与环境管理
- 建议区分开发、测试、生产三套环境。
- 发布采用版本号管理（例如 v0.2.0），并记录变更清单。
- 生产发布前执行发布检查单：数据库变更、环境变量、回滚方案。
- 线上问题处理遵循“先止损后修复”，保留操作与日志记录。

7. 安全与数据管理
- 用户密码必须迁移为哈希存储（bcrypt）。
- 权限接口默认拒绝，按角色白名单放行。
- 风险图片与日志数据设置最小可见范围，避免越权读取。
- 关键数据（用户、风险、打卡）建立定期备份策略。

8. 进度与风险管理
- 进度跟踪建议使用燃尽图或看板累计流图。
- 每周同步三类信息：已完成、阻塞项、下周计划。
- 风险项建立台账，包含：风险描述、影响范围、负责人、缓解措施。
- 对高风险功能（地图、上传、空间查询）提前做技术预研。

9. 团队协作机制
- 日常：每日 10-15 分钟站会，同步进展与阻塞。
- 周度：周计划会 + 周复盘会，形成可执行改进项。
- 里程碑：阶段结束后组织评审，按验收标准逐项过检。

10. 完成定义（Definition of Done）
- 功能代码完成并通过自测。
- 接口文档与设计书对应章节已更新。
- 关键场景测试通过，无 P0/P1 缺陷遗留。
- 代码已评审并合并，部署脚本或操作说明可执行。

十七、自动化构建与部署方法（CI/CD）
本节聚焦“自动化构建与部署”实践，说明当前已落地能力、标准流水线、环境策略与回滚机制。

1. 当前自动化能力现状
- 已完成：后端 Linux 二进制自动构建（GitHub Actions）。
- 已完成：后端 Docker 镜像自动构建并推送到 GHCR。
- 已完成：main 分支自动通过 SSH 拉起服务器容器部署。
- 未完成：Web 端自动构建与部署流水线。
- 未完成：App 端（Capacitor/Android）自动打包与发布流水线。
- 未完成：测试门禁（单测、接口测试）强制作为部署前置条件。

2. 已有流水线说明（仓库现状）
- 工作流 A：.github/workflows/build-backend-linux.yml
	用途：后端 Linux 构建产物（artifact）输出。
	触发：backend 目录变更或手动触发。
	关键步骤：安装依赖 -> 重编译 better-sqlite3 -> 执行 build:linux -> 上传 dist。

- 工作流 B：.github/workflows/build-backend-docker.yml
	用途：构建后端镜像并发布到 GHCR，main 分支自动部署。
	触发：backend 目录变更或手动触发。
	关键步骤：构建镜像 -> 推送 GHCR -> SSH 登录服务器 -> 停旧容器 -> 启新容器。

3. 推荐标准流水线（目标态）
1. 代码检查阶段（CI）
- 执行 lint、格式检查、基础静态扫描。
- 执行后端单元测试与关键接口测试。
- 结果要求：失败即中断，不进入构建阶段。

2. 构建阶段（CI）
- 后端：构建 Docker 镜像并生成版本标签（branch、sha、latest）。
- Web：执行 npm ci + build，生成静态资源。
- App：执行 Android Release 打包（AAB/APK）。

3. 制品管理阶段（CI）
- 后端镜像推送 GHCR。
- Web 制品上传制品库或对象存储。
- App 包上传内部发布渠道或制品平台。

4. 部署阶段（CD）
- 测试环境：分支或预发布触发自动部署。
- 生产环境：仅 main/tag 触发，建议保留人工确认步骤。
- 部署完成后执行健康检查（/me、/online-users 或自定义 health 接口）。

4. 环境与发布策略
- 环境分层：dev（开发）/staging（测试）/prod（生产）。
- 发布策略：先 staging 验证，再 prod 滚动发布。
- 版本策略：建议采用语义化版本（例如 v0.3.0）+ Git 提交哈希追踪。
- 配置策略：敏感信息全部放入 GitHub Secrets 或服务器环境变量。

5. 回滚与容灾策略
- 镜像回滚：保留最近 N 个稳定镜像标签，故障时回滚到上一个稳定版本。
- 数据回滚：数据库变更采用增量迁移脚本，禁止手工直改生产库结构。
- 回滚触发条件：健康检查失败、核心链路异常、错误率超阈值。
- 回滚责任：值班负责人在 10-15 分钟内完成止损与版本回退。

6. 部署安全与权限控制
- 部署账户使用最小权限原则，仅授予所需命令权限。
- SSH 私钥、GHCR 凭据、服务器地址等全部使用 Secrets 管理。
- 生产部署建议增加人工审批步骤，避免误发布。
- 部署日志与审计记录至少保留 90 天。

7. 下一步落地清单（自动化方向）
- P0：把 lint/test 加入现有后端工作流，形成质量门禁。
- P0：为 Web 端新增 build + deploy 工作流。
- P1：增加 staging 环境自动部署与 smoke test。
- P1：给生产部署增加手动批准与自动回滚脚本。
- P2：补齐 App 端自动打包与签名发布流水线。

